import UIKit
import UserNotifications
import WebKit
#if canImport(ScreenShield)
import ScreenShield
#endif

final class KADimensionTigerBessTS: UIViewController, WKNavigationDelegate, WKUIDelegate, WKScriptMessageHandler {
    private let KAZeroLiefChanceTS: URL
    private let KABoomArrivalDreamsTS: KADisMoonObsessionTS
    private let KAFuncVcGoldTS: KAUpLovingDogTS
    private let KAUpMaxDependTS: UIImage?
    private let KAJuiceComesAthleteTS: () -> Void
    private var KAArgFlyAnimalTS = false
    private var KAComplainArrestBiologicalTS = false
    private var KASpellCountAppleTS = false
    private var KABookgoOctopusTS = false
    private var KATrainHeroConsTS = false
    private var KAFreeChanceCityTS: Bool?
    private let KAHaveSeeTracksTS = UIView()
    private var KAFrogEnoughCryTS: UIView?
    private weak var KAVerClockTheTS: UIImageView?
    private let KACenterMirrorComputerTS = UIView()
    private let KADragonPantsBinTS = UIActivityIndicatorView(style: .large)
    private let KAWoodPassFlatTS = UILabel()
    private let KAGrazeColorsGrazeTS = UIButton(type: .system)
    private let KABusyJumpWantTS = UIView()
    private let KAArrestForestBoatTS = UIActivityIndicatorView(style: .large)
    private let KAColonialSwapOctopusTS = UILabel()
    private let KAZooLightDisagreeTS = UIView()
    private let KAHorseOnlyEraserTS = UILabel()
    private var KAKeyConsRiverTS: DispatchWorkItem?

    private let KAObsessionFileCollectTS: Set<String> = [
        "squarecash", "cashme", "upi", "phonepe", "paytmmp", "gpay",
        "com.amazon.mobile.shopping", "mobikwik", "freecharge"
    ]

    private lazy var KALiefReadBeeTS: WKWebView = {
        let KADimensionMobileUpTS = WKUserContentController()
        ["rechargePay", "Close", "openBrowser"].forEach { KADimensionMobileUpTS.add(self, name: $0) }
        let KAShoesPaintWindTS = WKWebViewConfiguration()
        KAShoesPaintWindTS.userContentController = KADimensionMobileUpTS
        KAShoesPaintWindTS.allowsInlineMediaPlayback = true
        KAShoesPaintWindTS.mediaTypesRequiringUserActionForPlayback = []
        let KASwordTracksScouringTS = WKWebView(frame: .zero, configuration: KAShoesPaintWindTS)
        KASwordTracksScouringTS.navigationDelegate = self
        KASwordTracksScouringTS.uiDelegate = self
        KASwordTracksScouringTS.isOpaque = false
        KASwordTracksScouringTS.backgroundColor = .clear
        KASwordTracksScouringTS.scrollView.backgroundColor = .clear
        KASwordTracksScouringTS.scrollView.contentInsetAdjustmentBehavior = .never
        KASwordTracksScouringTS.scrollView.contentInset = .zero
        KASwordTracksScouringTS.scrollView.scrollIndicatorInsets = .zero
        KASwordTracksScouringTS.scrollView.bounces = false
        KASwordTracksScouringTS.allowsBackForwardNavigationGestures = true
        return KASwordTracksScouringTS
    }()

    init(KAZeroLiefChanceTS: URL,
         KABoomArrivalDreamsTS: KADisMoonObsessionTS,
         KAFuncVcGoldTS: KAUpLovingDogTS,
         KAUpMaxDependTS: UIImage? = nil,
        KAJuiceComesAthleteTS: @escaping () -> Void) {
        self.KAZeroLiefChanceTS = KAZeroLiefChanceTS
        self.KABoomArrivalDreamsTS = KABoomArrivalDreamsTS
        self.KAFuncVcGoldTS = KAFuncVcGoldTS
        self.KAUpMaxDependTS = KAUpMaxDependTS
        self.KAJuiceComesAthleteTS = KAJuiceComesAthleteTS
        super.init(nibName: nil, bundle: nil)
    }

    required init?(coder: NSCoder) { fatalError("init(coder:) has not been implemented") }

    override func viewDidLoad() {
        super.viewDidLoad()
        KAKingOrLaterTS()
        KAOnResQuizzesTS()
        KAMorningBlightHeroTS()
        KASilverBlowKeyboardTS()
        KACloudBlindDisTS(KAPickWindPrincessTS: "Loading…")
        StoreKit1PurchaseManager.KAInterCentralDisplayTS.KASnowShieldTableTS(
            KABoomArrivalDreamsTS: KABoomArrivalDreamsTS,
            KAFuncVcGoldTS: KAFuncVcGoldTS
        ) { [weak self] KATigerCharacterMyTS in self?.KAConsUnevenlyShieldTS(KATigerCharacterMyTS) }
        KADespitePotionBigTS.KAInterCentralDisplayTS.KABoomGoldMountainTS("H5", "WebView 页面已创建，等待进入 Window 后加载业务页面")
    }

    override func viewWillAppear(_ animated: Bool) {
        super.viewWillAppear(animated)
        guard let KAFreeCablePhoneTS = navigationController else { return }
        if KAFreeChanceCityTS == nil {
            KAFreeChanceCityTS = KAFreeCablePhoneTS.isNavigationBarHidden
        }
        KAFreeCablePhoneTS.setNavigationBarHidden(true, animated: animated)
    }

    override func viewWillDisappear(_ animated: Bool) {
        super.viewWillDisappear(animated)
        guard let KAFreeCablePhoneTS = navigationController,
              let KAFreeChanceCityTS else { return }
        KAFreeCablePhoneTS.setNavigationBarHidden(KAFreeChanceCityTS, animated: animated)
    }

    override func viewDidAppear(_ animated: Bool) {
        super.viewDidAppear(animated)
        KAPhotoDirectorShadowTS()
        if !KABookgoOctopusTS {
            KABookgoOctopusTS = true
            #if canImport(ScreenShield)
            ScreenShield.shared.protectFromScreenRecording()
            KADespitePotionBigTS.KAInterCentralDisplayTS.KABoomGoldMountainTS("ScreenShield", "已启用录屏检测；截屏保护由独立 H5 安全容器承担")
            #else
            KADespitePotionBigTS.KAInterCentralDisplayTS.KABoomGoldMountainTS("ScreenShield", "未安装 ScreenShield，H5 使用普通内容容器")
            #endif
        }
        KAArgDeeplySlimeTS()
    }

    private func KAArgDeeplySlimeTS() {
        guard KAArgFlyAnimalTS, !KASpellCountAppleTS else { return }
        KASpellCountAppleTS = true
        UNUserNotificationCenter.current().requestAuthorization(options: [.alert, .sound, .badge]) { KAOtherResVerTS, KAIndexChangeBridgeTS in
            if let KAIndexChangeBridgeTS { KADespitePotionBigTS.KAInterCentralDisplayTS.KABoomGoldMountainTS("推送权限", KAIndexChangeBridgeTS.localizedDescription) }
            if KAOtherResVerTS { DispatchQueue.main.async { UIApplication.shared.registerForRemoteNotifications() } }
        }
    }

    deinit {
        KALiefReadBeeTS.configuration.userContentController.removeAllScriptMessageHandlers()
        StoreKit1PurchaseManager.KAInterCentralDisplayTS.KAAversionBirthPicTS()
    }

    private func KAKingOrLaterTS() {
        // 安全容器在系统截屏中会隐藏其内容，根视图就是最终截图底色。
        // 使用固定白色，避免深色模式或 systemGroupedBackground 产生灰/黑底。
        view.backgroundColor = .white
        KABikeArrestMirrorTS()
        if let KAUpMaxDependTS {
            let KAMountainAskBusyTS = UIImageView(image: KAUpMaxDependTS)
            KAVerClockTheTS = KAMountainAskBusyTS
            KAMountainAskBusyTS.translatesAutoresizingMaskIntoConstraints = false
            KAMountainAskBusyTS.contentMode = .scaleAspectFill
            KAHaveSeeTracksTS.addSubview(KAMountainAskBusyTS)
            NSLayoutConstraint.activate([
                KAMountainAskBusyTS.leadingAnchor.constraint(equalTo: KAHaveSeeTracksTS.leadingAnchor),
                KAMountainAskBusyTS.trailingAnchor.constraint(equalTo: KAHaveSeeTracksTS.trailingAnchor),
                KAMountainAskBusyTS.topAnchor.constraint(equalTo: KAHaveSeeTracksTS.topAnchor),
                KAMountainAskBusyTS.bottomAnchor.constraint(equalTo: KAHaveSeeTracksTS.bottomAnchor)
            ])
        }
        KALiefReadBeeTS.translatesAutoresizingMaskIntoConstraints = false
        KAHaveSeeTracksTS.addSubview(KALiefReadBeeTS)
        NSLayoutConstraint.activate([
            KALiefReadBeeTS.leadingAnchor.constraint(equalTo: KAHaveSeeTracksTS.leadingAnchor),
            KALiefReadBeeTS.trailingAnchor.constraint(equalTo: KAHaveSeeTracksTS.trailingAnchor),
            KALiefReadBeeTS.topAnchor.constraint(equalTo: KAHaveSeeTracksTS.topAnchor),
            KALiefReadBeeTS.bottomAnchor.constraint(equalTo: KAHaveSeeTracksTS.bottomAnchor)
        ])
    }

    private func KABikeArrestMirrorTS() {
        #if canImport(ScreenShield)
        let KAStartWoodTerTS = ScreenshotProtectingView(contentView: KAHaveSeeTracksTS)
        KAStartWoodTerTS.translatesAutoresizingMaskIntoConstraints = false
        view.addSubview(KAStartWoodTerTS)
        NSLayoutConstraint.activate([
            KAStartWoodTerTS.leadingAnchor.constraint(equalTo: view.leadingAnchor),
            KAStartWoodTerTS.trailingAnchor.constraint(equalTo: view.trailingAnchor),
            KAStartWoodTerTS.topAnchor.constraint(equalTo: view.topAnchor),
            KAStartWoodTerTS.bottomAnchor.constraint(equalTo: view.bottomAnchor)
        ])

        if KAHaveSeeTracksTS.superview != nil {
            KAFrogEnoughCryTS = KAStartWoodTerTS
            KADespitePotionBigTS.KAInterCentralDisplayTS.KABoomGoldMountainTS("ScreenShield", "H5 已放入独立安全容器；未修改控制器根 view.layer")
            return
        }

        KAStartWoodTerTS.removeFromSuperview()
        KADespitePotionBigTS.KAInterCentralDisplayTS.KABoomGoldMountainTS("ScreenShield", "当前系统无法创建安全容器，已降级为正常 H5，避免黑屏")
        #endif

        KAHaveSeeTracksTS.translatesAutoresizingMaskIntoConstraints = false
        view.addSubview(KAHaveSeeTracksTS)
        NSLayoutConstraint.activate([
            KAHaveSeeTracksTS.leadingAnchor.constraint(equalTo: view.leadingAnchor),
            KAHaveSeeTracksTS.trailingAnchor.constraint(equalTo: view.trailingAnchor),
            KAHaveSeeTracksTS.topAnchor.constraint(equalTo: view.topAnchor),
            KAHaveSeeTracksTS.bottomAnchor.constraint(equalTo: view.bottomAnchor)
        ])
    }

    private func KAOnResQuizzesTS() {
        KACenterMirrorComputerTS.translatesAutoresizingMaskIntoConstraints = false
        KACenterMirrorComputerTS.backgroundColor = .clear
        KACenterMirrorComputerTS.isHidden = true
        KADragonPantsBinTS.translatesAutoresizingMaskIntoConstraints = false
        KADragonPantsBinTS.color = .black
        KAWoodPassFlatTS.text = "Loading…"
        KAWoodPassFlatTS.textColor = .black
        KAWoodPassFlatTS.numberOfLines = 0
        KAWoodPassFlatTS.textAlignment = .center
        KAGrazeColorsGrazeTS.setTitle("Reload", for: .normal)
        KAGrazeColorsGrazeTS.isHidden = true
        KAGrazeColorsGrazeTS.addTarget(self, action: #selector(KABloomConsCoffeeTS), for: .touchUpInside)
        let KAArgSweetBeeTS = UIStackView(arrangedSubviews: [KADragonPantsBinTS, KAWoodPassFlatTS, KAGrazeColorsGrazeTS])
        KAArgSweetBeeTS.axis = .vertical
        KAArgSweetBeeTS.alignment = .center
        KAArgSweetBeeTS.spacing = 16
        KAArgSweetBeeTS.translatesAutoresizingMaskIntoConstraints = false
        KAHaveSeeTracksTS.addSubview(KACenterMirrorComputerTS)
        KACenterMirrorComputerTS.addSubview(KAArgSweetBeeTS)
        NSLayoutConstraint.activate([
            KACenterMirrorComputerTS.leadingAnchor.constraint(equalTo: KAHaveSeeTracksTS.leadingAnchor),
            KACenterMirrorComputerTS.trailingAnchor.constraint(equalTo: KAHaveSeeTracksTS.trailingAnchor),
            KACenterMirrorComputerTS.topAnchor.constraint(equalTo: KAHaveSeeTracksTS.topAnchor),
            KACenterMirrorComputerTS.bottomAnchor.constraint(equalTo: KAHaveSeeTracksTS.bottomAnchor),
            KAArgSweetBeeTS.centerXAnchor.constraint(equalTo: KACenterMirrorComputerTS.centerXAnchor),
            KAArgSweetBeeTS.centerYAnchor.constraint(equalTo: KACenterMirrorComputerTS.centerYAnchor),
            KAArgSweetBeeTS.leadingAnchor.constraint(greaterThanOrEqualTo: KACenterMirrorComputerTS.leadingAnchor, constant: 32),
            KAArgSweetBeeTS.trailingAnchor.constraint(lessThanOrEqualTo: KACenterMirrorComputerTS.trailingAnchor, constant: -32)
        ])
    }

    private func KAPhotoDirectorShadowTS() {
        guard !KAComplainArrestBiologicalTS, view.window != nil else { return }
        KAComplainArrestBiologicalTS = true
        KADespitePotionBigTS.KAInterCentralDisplayTS.KABoomGoldMountainTS("H5", "WebView 已进入 Window，开始加载加密业务页面")
        KALiefReadBeeTS.load(URLRequest(url: KAZeroLiefChanceTS))
    }

    func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
        KADragonPantsBinTS.stopAnimating()
        KACenterMirrorComputerTS.isHidden = true
        KAGrazeColorsGrazeTS.isHidden = true
        guard !KAArgFlyAnimalTS else {
            KADespitePotionBigTS.KAInterCentralDisplayTS.KABoomGoldMountainTS("H5", "页面重新加载完成")
            return
        }
        KAVerClockTheTS?.removeFromSuperview()
        KADespitePotionBigTS.KAInterCentralDisplayTS.KABoomGoldMountainTS("H5", "首次页面加载完成；已移除 H5 Loading 背景，避免透明区域继续显示登录底图")
        KAArgFlyAnimalTS = true
        KADespitePotionBigTS.KAInterCentralDisplayTS.KABoomGoldMountainTS("H5", "首次页面加载完成，隐藏 Loading 并显示 H5")
        KAArgDeeplySlimeTS()
    }

    func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
        KADespitePotionBigTS.KAInterCentralDisplayTS.KABoomGoldMountainTS("JS Bridge", "收到 \(message.name)")
        switch message.name {
        case "Close":
            guard !KATrainHeroConsTS else {
                KADespitePotionBigTS.KAInterCentralDisplayTS.KABoomGoldMountainTS("退出登录", "已在处理 Close，忽略 H5 重复回调")
                return
            }
            KATrainHeroConsTS = true
            userContentController.removeScriptMessageHandler(forName: "Close")
            KALiefReadBeeTS.stopLoading()
            KADespitePotionBigTS.KAInterCentralDisplayTS.KABoomGoldMountainTS("退出登录", "首次收到 Close，开始单次返回快速登录页")
            KAJuiceComesAthleteTS()
        case "openBrowser":
            if let KABarrenAthleteBrushTS = message.body as? [String: Any], let KAAppleCloudCoatTS = KABarrenAthleteBrushTS["url"] as? String {
                KADolphinDestroyListTS(KAAppleCloudCoatTS)
            } else if let KAAppleCloudCoatTS = message.body as? String {
                KADolphinDestroyListTS(KAAppleCloudCoatTS)
            }
        case "rechargePay":
            guard let KABarrenAthleteBrushTS = message.body as? [String: Any] else { return }
            let KAShirtDreamsSixTS = KABarrenAthleteBrushTS["batchNo"] as? String ?? ""
            let KAGoodNetCoffeeTS = KABarrenAthleteBrushTS["orderCode"] as? String ?? ""
            guard !KAShirtDreamsSixTS.isEmpty, !KAGoodNetCoffeeTS.isEmpty else {
                KACenterBlowRainTS(KACarShadowCenterTS: "Payment Failed", KAPickWindPrincessTS: "The payment information is incomplete.")
                return
            }
            // 严格要求：用户点击购买、收到有效 rechargePay 时立即触发 InitiateCheckout。
            KAFuncVcGoldTS.KACivilianSwordPaperTS(.KACarDrpWaterTS)
            StoreKit1PurchaseManager.KAInterCentralDisplayTS.KAScreenSlimeNetTS(
                KAShirtDreamsSixTS: KAShirtDreamsSixTS,
                KAGoodNetCoffeeTS: KAGoodNetCoffeeTS
            )
        default:
            break
        }
    }

    private func KAMorningBlightHeroTS() {
        KABusyJumpWantTS.translatesAutoresizingMaskIntoConstraints = false
        KABusyJumpWantTS.backgroundColor = UIColor.black.withAlphaComponent(0.55)
        KABusyJumpWantTS.isHidden = true
        KAColonialSwapOctopusTS.translatesAutoresizingMaskIntoConstraints = false
        KAColonialSwapOctopusTS.textColor = .white
        KAColonialSwapOctopusTS.numberOfLines = 0
        KAColonialSwapOctopusTS.textAlignment = .center
        KAArrestForestBoatTS.translatesAutoresizingMaskIntoConstraints = false
        KAArrestForestBoatTS.color = .white
        KAHaveSeeTracksTS.addSubview(KABusyJumpWantTS)
        KABusyJumpWantTS.addSubview(KAArrestForestBoatTS)
        KABusyJumpWantTS.addSubview(KAColonialSwapOctopusTS)
        NSLayoutConstraint.activate([
            KABusyJumpWantTS.leadingAnchor.constraint(equalTo: KAHaveSeeTracksTS.leadingAnchor),
            KABusyJumpWantTS.trailingAnchor.constraint(equalTo: KAHaveSeeTracksTS.trailingAnchor),
            KABusyJumpWantTS.topAnchor.constraint(equalTo: KAHaveSeeTracksTS.topAnchor),
            KABusyJumpWantTS.bottomAnchor.constraint(equalTo: KAHaveSeeTracksTS.bottomAnchor),
            KAArrestForestBoatTS.centerXAnchor.constraint(equalTo: KABusyJumpWantTS.centerXAnchor),
            KAArrestForestBoatTS.centerYAnchor.constraint(equalTo: KABusyJumpWantTS.centerYAnchor, constant: -24),
            KAColonialSwapOctopusTS.topAnchor.constraint(equalTo: KAArrestForestBoatTS.bottomAnchor, constant: 16),
            KAColonialSwapOctopusTS.leadingAnchor.constraint(equalTo: KABusyJumpWantTS.leadingAnchor, constant: 30),
            KAColonialSwapOctopusTS.trailingAnchor.constraint(equalTo: KABusyJumpWantTS.trailingAnchor, constant: -30)
        ])
    }

    private func KAConsUnevenlyShieldTS(_ KATigerCharacterMyTS: StoreKit1PurchaseManager.KABinMomentsFuncTS) {
        switch KATigerCharacterMyTS {
        case .KAArrestBigBinTS(let KAPickWindPrincessTS):
            KABusyJumpWantTS.isHidden = false
            KAColonialSwapOctopusTS.text = KAPickWindPrincessTS
            KAArrestForestBoatTS.startAnimating()
        case .KALabelDegreeWoodTS:
            KARainLoadGrapeTS()
            KASeabedMomentMouseTS(KAPickWindPrincessTS: "Payment successful")
        case .KACallLineHatTS(let KAPickWindPrincessTS):
            KARainLoadGrapeTS()
            KACenterBlowRainTS(KACarShadowCenterTS: "Payment Failed", KAPickWindPrincessTS: KAPickWindPrincessTS)
        case .KASeeLaptopPerfectTS:
            KARainLoadGrapeTS()
            KASeabedMomentMouseTS(KAPickWindPrincessTS: "Payment cancelled")
        }
    }

    private func KARainLoadGrapeTS() {
        KAArrestForestBoatTS.stopAnimating()
        KABusyJumpWantTS.isHidden = true
    }

    private func KACenterBlowRainTS(KACarShadowCenterTS: String, KAPickWindPrincessTS: String) {
        guard presentedViewController == nil else { return }
        let KAGliderBlindOnlineTS = UIAlertController(title: KACarShadowCenterTS, message: KAPickWindPrincessTS, preferredStyle: .alert)
        KAGliderBlindOnlineTS.addAction(UIAlertAction(title: "OK", style: .default))
        present(KAGliderBlindOnlineTS, animated: true)
    }

    private func KASilverBlowKeyboardTS() {
        KAZooLightDisagreeTS.translatesAutoresizingMaskIntoConstraints = false
        KAZooLightDisagreeTS.backgroundColor = UIColor.black.withAlphaComponent(0.82)
        KAZooLightDisagreeTS.layer.cornerRadius = 12
        KAZooLightDisagreeTS.alpha = 0
        KAZooLightDisagreeTS.isHidden = true

        KAHorseOnlyEraserTS.translatesAutoresizingMaskIntoConstraints = false
        KAHorseOnlyEraserTS.textColor = .white
        KAHorseOnlyEraserTS.font = .systemFont(ofSize: 15, weight: .semibold)
        KAHorseOnlyEraserTS.numberOfLines = 0
        KAHorseOnlyEraserTS.textAlignment = .center

        KAHaveSeeTracksTS.addSubview(KAZooLightDisagreeTS)
        KAZooLightDisagreeTS.addSubview(KAHorseOnlyEraserTS)
        NSLayoutConstraint.activate([
            KAZooLightDisagreeTS.centerXAnchor.constraint(equalTo: KAHaveSeeTracksTS.centerXAnchor),
            KAZooLightDisagreeTS.bottomAnchor.constraint(equalTo: KAHaveSeeTracksTS.safeAreaLayoutGuide.bottomAnchor, constant: -64),
            KAZooLightDisagreeTS.leadingAnchor.constraint(greaterThanOrEqualTo: KAHaveSeeTracksTS.leadingAnchor, constant: 32),
            KAZooLightDisagreeTS.trailingAnchor.constraint(lessThanOrEqualTo: KAHaveSeeTracksTS.trailingAnchor, constant: -32),
            KAHorseOnlyEraserTS.leadingAnchor.constraint(equalTo: KAZooLightDisagreeTS.leadingAnchor, constant: 20),
            KAHorseOnlyEraserTS.trailingAnchor.constraint(equalTo: KAZooLightDisagreeTS.trailingAnchor, constant: -20),
            KAHorseOnlyEraserTS.topAnchor.constraint(equalTo: KAZooLightDisagreeTS.topAnchor, constant: 12),
            KAHorseOnlyEraserTS.bottomAnchor.constraint(equalTo: KAZooLightDisagreeTS.bottomAnchor, constant: -12)
        ])
    }

    private func KASeabedMomentMouseTS(KAPickWindPrincessTS: String) {
        KAKeyConsRiverTS?.cancel()
        KAHorseOnlyEraserTS.text = KAPickWindPrincessTS
        KAZooLightDisagreeTS.isHidden = false
        KAHaveSeeTracksTS.bringSubviewToFront(KAZooLightDisagreeTS)
        UIView.animate(withDuration: 0.2) {
            self.KAZooLightDisagreeTS.alpha = 1
        }

        let KAShadowDisplayAndTS = DispatchWorkItem { [weak self] in
            guard let self else { return }
            UIView.animate(withDuration: 0.2, animations: {
                self.KAZooLightDisagreeTS.alpha = 0
            }, completion: { _ in
                self.KAZooLightDisagreeTS.isHidden = true
            })
        }
        KAKeyConsRiverTS = KAShadowDisplayAndTS
        DispatchQueue.main.asyncAfter(deadline: .now() + 2, execute: KAShadowDisplayAndTS)
    }

    func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {
        KAScreenJoyWindTS(error)
    }

    func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
        KAScreenJoyWindTS(error)
    }

    func webViewWebContentProcessDidTerminate(_ webView: WKWebView) {
        KADespitePotionBigTS.KAInterCentralDisplayTS.KABoomGoldMountainTS("H5错误", "WebContent 进程终止，开始自动恢复")
        KACloudBlindDisTS(KAPickWindPrincessTS: "页面正在恢复…")
        webView.reload()
    }

    private func KAScreenJoyWindTS(_ KAIndexChangeBridgeTS: Error) {
        let KAResPaperRainTS = KAIndexChangeBridgeTS as NSError
        guard KAResPaperRainTS.domain != NSURLErrorDomain || KAResPaperRainTS.code != NSURLErrorCancelled else { return }
        KADespitePotionBigTS.KAInterCentralDisplayTS.KABoomGoldMountainTS("H5错误", KAIndexChangeBridgeTS.localizedDescription)
        KADragonPantsBinTS.stopAnimating()
        KAWoodPassFlatTS.text = "页面加载失败\n\(KAIndexChangeBridgeTS.localizedDescription)"
        KAGrazeColorsGrazeTS.isHidden = false
        KACenterMirrorComputerTS.isHidden = false
    }

    private func KACloudBlindDisTS(KAPickWindPrincessTS: String) {
        KAWoodPassFlatTS.text = KAPickWindPrincessTS
        KAGrazeColorsGrazeTS.isHidden = true
        KACenterMirrorComputerTS.isHidden = false
        KADragonPantsBinTS.startAnimating()
    }

    @objc private func KABloomConsCoffeeTS() {
        KACloudBlindDisTS(KAPickWindPrincessTS: "页面加载中…")
        if KALiefReadBeeTS.url != nil {
            KALiefReadBeeTS.reload()
        } else {
            KALiefReadBeeTS.load(URLRequest(url: KAZeroLiefChanceTS))
        }
    }

    private func KADolphinDestroyListTS(_ KAAppleCloudCoatTS: String) {
        guard let KAZeroLiefChanceTS = URL(string: KAAppleCloudCoatTS) else { return }
        let KADelMsgBinTS = KAZeroLiefChanceTS.scheme?.lowercased() ?? ""
        if KAObsessionFileCollectTS.contains(KADelMsgBinTS) {
            return
        }
        UIApplication.shared.open(KAZeroLiefChanceTS) { [weak self] KALabelDegreeWoodTS in
            let KADemonstrateFromTreeTS = try? JSONSerialization.data(withJSONObject: ["state": KALabelDegreeWoodTS ? "success" : "failed", "url": KAAppleCloudCoatTS])
            let KAInterCivilianBusTS = KADemonstrateFromTreeTS.flatMap { String(data: $0, encoding: .utf8) } ?? "{}"
            self?.KALiefReadBeeTS.evaluateJavaScript("window.dispatchEvent(new CustomEvent('nativeOpenState',{detail:\(KAInterCivilianBusTS)}));")
        }
    }

    func webView(_ webView: WKWebView, decidePolicyFor action: WKNavigationAction,
                 decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
        guard let KAZeroLiefChanceTS = action.request.url else { decisionHandler(.cancel); return }
        let KADelMsgBinTS = KAZeroLiefChanceTS.scheme?.lowercased() ?? ""
        if KAZeroLiefChanceTS.host?.lowercased() == "apps.apple.com" || KADelMsgBinTS == "itms-apps" {
            KADolphinDestroyListTS(KAZeroLiefChanceTS.absoluteString)
            decisionHandler(.cancel)
        } else if !["http", "https", "file", "about"].contains(KADelMsgBinTS) {
            KADolphinDestroyListTS(KAZeroLiefChanceTS.absoluteString)
            decisionHandler(.cancel)
        } else {
            decisionHandler(.allow)
        }
    }

    func webView(_ webView: WKWebView, createWebViewWith configuration: WKWebViewConfiguration,
                 for action: WKNavigationAction, windowFeatures: WKWindowFeatures) -> WKWebView? {
        if let KAZeroLiefChanceTS = action.request.url {
            let KADelMsgBinTS = KAZeroLiefChanceTS.scheme?.lowercased() ?? ""
            if KAObsessionFileCollectTS.contains(KADelMsgBinTS) {
                return nil
            } else if KAZeroLiefChanceTS.host?.lowercased() == "apps.apple.com" {
                KADolphinDestroyListTS(KAZeroLiefChanceTS.absoluteString)
            } else {
                webView.load(URLRequest(url: KAZeroLiefChanceTS))
            }
        }
        return nil
    }

    @available(iOS 15.0, *)
    func webView(_ webView: WKWebView, requestMediaCapturePermissionFor origin: WKSecurityOrigin,
                 initiatedByFrame frame: WKFrameInfo, type: WKMediaCaptureType,
                 decisionHandler: @escaping (WKPermissionDecision) -> Void) {
        decisionHandler(.grant)
    }
}
